//Os :- operationg system module 
//used for to see ram and os info.

const os=require("os");
//console.log(os.arch());
//console.log(os.freemem());
//console.log(os.platform());
console.log(os.userInfo()); 
